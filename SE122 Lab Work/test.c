#include <stdio.h>
int main()
{
    char var_1, var_2;
    int a, b, sum; //4 bit
    a = 87;
    b = 50;
    sum = a+b;
    printf("value 1: %d, value 2: %d, sum: %d", a, b, sum);
    int intt = 10;
    printf("My name is Jubaidul");

    // printf("Please enter a number between (10-99): ");
    // scanf("%c %c", &var_1, &var_2);

    // int num = (var_1 - 48) * 10 + (var_2 - 48);
    // printf("The result as integer is: %d", num);

    return 0;
}